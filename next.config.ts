import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Os JSONs de dados são importados diretamente como módulos
  // Sem necessidade de configuração extra para o MVP
};

export default nextConfig;
